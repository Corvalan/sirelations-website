---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Superintelligence Relations" description="SiRelations is an AI safety and research organization working on human oversight of advanced AI systems.">
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <h1 class="hero-title">AI research that puts human oversight at the frontier</h1>
        <p class="hero-description">
          SiRelations is an AI safety research organization. We study how humans can maintain meaningful agency when working with systems that exceed human cognitive capabilities.
        </p>
        <div class="hero-actions">
          <a href="/about" class="btn btn-primary">Learn more</a>
          <a href="/services" class="btn btn-secondary">Our work</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Purpose Section -->
  <section class="section">
    <div class="container">
      <div class="purpose-content">
        <h2>Making human oversight meaningful</h2>
        <p class="purpose-lead">
          As AI systems grow more capable, the question isn't whether humans remain "in the loop" — it's whether that presence translates to genuine agency. We conduct foundational research on discernment: the capacity to remain clear, responsible, and sovereign when collaborating with advanced AI.
        </p>
      </div>
    </div>
  </section>

  <!-- Research Areas -->
  <section class="section section-alt">
    <div class="container">
      <h2 class="section-title">Research</h2>
      <p class="section-subtitle">Our work spans measurement, policy, and intervention design.</p>

      <div class="research-cards">
        <a href="/about" class="research-card">
          <h3>Oversight Measurement</h3>
          <p>Developing empirical methods to assess whether humans can effectively exercise oversight in practice — not just in theory.</p>
        </a>

        <a href="/about" class="research-card">
          <h3>Discernment Science</h3>
          <p>Studying the cognitive and contextual factors that enable or undermine human judgment when working with AI systems.</p>
        </a>

        <a href="/about" class="research-card">
          <h3>Policy Translation</h3>
          <p>Bridging research findings to regulatory frameworks like the EU AI Act, where human oversight requirements lack measurement methods.</p>
        </a>

        <a href="/services" class="research-card">
          <h3>Applied Assessment</h3>
          <p>Translating foundational research into practical tools for organizations navigating AI governance requirements.</p>
        </a>
      </div>
    </div>
  </section>

  <!-- The Problem -->
  <section class="section">
    <div class="container">
      <div class="problem-grid">
        <div class="problem-content">
          <h2>The measurement gap</h2>
          <p>
            Regulatory frameworks now mandate human oversight of AI systems. The EU AI Act requires humans to "correctly interpret" AI output and decide when to override. Impact assessments identify risks before deployment.
          </p>
          <p>
            But these frameworks assume capacities that lack established measurement methods. Compliance asks whether oversight exists. Our research asks whether the humans providing it can do so meaningfully.
          </p>
        </div>
        <div class="problem-visual">
          <div class="visual-block"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- Featured Work -->
  <section class="section section-alt">
    <div class="container">
      <h2 class="section-title">Featured</h2>

      <div class="featured-grid">
        <article class="featured-card featured-large">
          <div class="featured-content">
            <span class="featured-tag">Research</span>
            <h3>Discernment: A Framework for Human Oversight Capacity</h3>
            <p>Introducing discernment as a measurable construct for evaluating whether humans can maintain agency when working with advanced AI systems.</p>
          </div>
        </article>

        <article class="featured-card">
          <div class="featured-content">
            <span class="featured-tag">Applied</span>
            <h3>Discernment Snapshot</h3>
            <p>A focused assessment tool for organizations preparing for AI governance requirements.</p>
          </div>
        </article>

        <article class="featured-card">
          <div class="featured-content">
            <span class="featured-tag">Policy</span>
            <h3>EU AI Act Analysis</h3>
            <p>How Article 14's oversight requirements create demand for measurement methods we don't yet have.</p>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="section section-cta">
    <div class="container">
      <div class="cta-content">
        <h2>Work with us</h2>
        <p>We partner with organizations preparing for meaningful AI governance.</p>
        <a href="/contact" class="btn btn-primary">Get in touch</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero */
  .hero {
    padding: var(--space-2xl) 0 var(--space-xl);
  }

  .hero-content {
    max-width: 900px;
  }

  .hero-title {
    font-size: var(--font-size-display);
    line-height: 1.05;
    letter-spacing: -0.03em;
    margin-bottom: var(--space-lg);
  }

  .hero-description {
    font-size: var(--font-size-xl);
    line-height: 1.6;
    max-width: 720px;
    margin-bottom: var(--space-lg);
  }

  .hero-actions {
    display: flex;
    gap: var(--space-sm);
  }

  /* Purpose */
  .purpose-content {
    max-width: 800px;
  }

  .purpose-content h2 {
    margin-bottom: var(--space-md);
  }

  .purpose-lead {
    font-size: var(--font-size-lg);
    line-height: 1.7;
  }

  /* Section styles */
  .section-title {
    margin-bottom: var(--space-xs);
  }

  .section-subtitle {
    font-size: var(--font-size-lg);
    margin-bottom: var(--space-xl);
    max-width: 600px;
  }

  .section-alt {
    background: var(--color-bg-secondary);
  }

  /* Research Cards */
  .research-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-md);
  }

  .research-card {
    padding: var(--space-lg);
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    text-decoration: none;
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .research-card:hover {
    border-color: var(--color-text-muted);
    transform: translateY(-2px);
  }

  .research-card h3 {
    font-size: var(--font-size-xl);
    margin-bottom: var(--space-sm);
    color: var(--color-text);
  }

  .research-card p {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    margin: 0;
  }

  /* Problem Grid */
  .problem-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
    align-items: center;
  }

  .problem-content h2 {
    margin-bottom: var(--space-md);
  }

  .problem-content p {
    margin-bottom: var(--space-md);
  }

  .problem-content p:last-child {
    margin-bottom: 0;
  }

  .problem-visual {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .visual-block {
    width: 100%;
    max-width: 400px;
    aspect-ratio: 4/3;
    background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-accent-light) 100%);
    border-radius: 12px;
    opacity: 0.9;
  }

  /* Featured Grid */
  .featured-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-md);
  }

  .featured-large {
    grid-column: 1 / -1;
  }

  .featured-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    overflow: hidden;
    transition: border-color 0.2s ease;
  }

  .featured-card:hover {
    border-color: var(--color-text-muted);
  }

  .featured-content {
    padding: var(--space-lg);
  }

  .featured-tag {
    display: inline-block;
    font-family: var(--font-ui);
    font-size: var(--font-size-xs);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-accent);
    margin-bottom: var(--space-sm);
  }

  .featured-card h3 {
    font-size: var(--font-size-2xl);
    margin-bottom: var(--space-sm);
    color: var(--color-text);
  }

  .featured-large h3 {
    font-size: var(--font-size-3xl);
  }

  .featured-card p {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    margin: 0;
  }

  /* CTA */
  .section-cta {
    text-align: center;
    border-top: 1px solid var(--color-border);
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-content h2 {
    margin-bottom: var(--space-md);
  }

  .cta-content p {
    font-size: var(--font-size-lg);
    margin-bottom: var(--space-lg);
    margin-left: auto;
    margin-right: auto;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .research-cards {
      gap: var(--space-sm);
    }

    .problem-grid {
      gap: var(--space-lg);
    }
  }

  @media (max-width: 768px) {
    .hero-actions {
      flex-direction: column;
    }

    .research-cards {
      grid-template-columns: 1fr;
    }

    .problem-grid {
      grid-template-columns: 1fr;
    }

    .problem-visual {
      order: -1;
    }

    .featured-grid {
      grid-template-columns: 1fr;
    }

    .featured-large {
      grid-column: 1;
    }
  }
</style>
