---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Services" description="Applied research tools for organizations navigating AI governance requirements. Our assessments measure human oversight capacity.">
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <h1>Applied research for AI governance</h1>
        <p class="hero-description">
          We translate our foundational research into practical tools for organizations preparing for AI governance requirements. Our assessments measure what regulations assume but don't test: whether humans can actually exercise meaningful oversight.
        </p>
      </div>
    </div>
  </section>

  <!-- Primary Product -->
  <section class="section">
    <div class="container">
      <div class="product-grid">
        <div class="product-content">
          <span class="product-tag">Assessment</span>
          <h2>Discernment Snapshot</h2>
          <p>
            A focused assessment of human oversight capacity in one concrete decision context where AI systems play a role. Not a compliance checklist — a measurement of whether the humans involved can actually steer.
          </p>
          <p>
            The Snapshot examines how decision-making operates under AI influence, identifies vulnerabilities where oversight breaks down, and provides actionable recommendations for strengthening discernment.
          </p>
          <a href="https://discernment.ai" class="btn btn-primary" rel="noopener">
            Learn more at discernment.ai
          </a>
        </div>
        <div class="product-specs">
          <div class="spec-item">
            <span class="spec-label">Scope</span>
            <span class="spec-value">One decision context</span>
          </div>
          <div class="spec-item">
            <span class="spec-label">Duration</span>
            <span class="spec-value">72 hours</span>
          </div>
          <div class="spec-item">
            <span class="spec-label">Output</span>
            <span class="spec-value">Assessment memo + Checklist</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- What We Assess -->
  <section class="section section-alt">
    <div class="container">
      <h2 class="section-title">What we assess</h2>
      <p class="section-subtitle">The Snapshot examines four dimensions of human-AI interaction.</p>

      <div class="assess-grid">
        <article class="assess-card">
          <h3>Influence recognition</h3>
          <p>Can humans identify when and how AI systems shape their thinking? We test awareness of influence patterns as they occur.</p>
        </article>

        <article class="assess-card">
          <h3>Override capacity</h3>
          <p>When AI recommendations conflict with human judgment, what happens? We examine the practical conditions for meaningful disagreement.</p>
        </article>

        <article class="assess-card">
          <h3>Responsibility tracking</h3>
          <p>Where does accountability live in AI-assisted decisions? We map how responsibility diffuses across human-AI workflows.</p>
        </article>

        <article class="assess-card">
          <h3>Intervention points</h3>
          <p>What mechanisms restore human agency when oversight weakens? We identify concrete leverage points for strengthening discernment.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- Regulatory Context -->
  <section class="section">
    <div class="container">
      <div class="context-grid">
        <div class="context-content">
          <h2>Regulatory alignment</h2>
          <p>
            The EU AI Act requires human oversight of high-risk AI systems. Article 14 mandates that humans can "correctly interpret" AI output and decide when to override. Article 4 requires AI literacy.
          </p>
          <p>
            These requirements assume capacities that regulations don't test. Our assessments address that gap — measuring whether designated overseers have the capacity to do what compliance frameworks assume.
          </p>
        </div>
        <div class="context-cards">
          <div class="context-card">
            <h4>EU AI Act</h4>
            <p>Article 14 oversight requirements</p>
          </div>
          <div class="context-card">
            <h4>AI Literacy</h4>
            <p>Article 4 capability mandates</p>
          </div>
          <div class="context-card">
            <h4>Impact Assessment</h4>
            <p>FRAIA and similar frameworks</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="section section-cta">
    <div class="container">
      <div class="cta-content">
        <h2>Get started</h2>
        <p>Contact us to discuss whether a Discernment Snapshot fits your situation.</p>
        <a href="/contact" class="btn btn-primary">Get in touch</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero */
  .hero {
    padding: var(--space-2xl) 0 var(--space-xl);
  }

  .hero-content {
    max-width: 800px;
  }

  .hero h1 {
    font-size: var(--font-size-display);
    line-height: 1.05;
    letter-spacing: -0.03em;
    margin-bottom: var(--space-lg);
  }

  .hero-description {
    font-size: var(--font-size-xl);
    line-height: 1.6;
  }

  /* Product Grid */
  .product-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: var(--space-xl);
    align-items: start;
  }

  .product-tag {
    display: inline-block;
    font-family: var(--font-ui);
    font-size: var(--font-size-xs);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-accent);
    margin-bottom: var(--space-sm);
  }

  .product-content h2 {
    margin-bottom: var(--space-md);
  }

  .product-content p {
    margin-bottom: var(--space-md);
  }

  .product-content .btn {
    margin-top: var(--space-sm);
  }

  .product-specs {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: var(--space-lg);
  }

  .spec-item {
    display: flex;
    justify-content: space-between;
    padding: var(--space-sm) 0;
    border-bottom: 1px solid var(--color-border);
  }

  .spec-item:last-child {
    border-bottom: none;
  }

  .spec-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  .spec-value {
    font-size: var(--font-size-sm);
    color: var(--color-text);
    font-weight: 500;
  }

  /* Section styles */
  .section-title {
    margin-bottom: var(--space-xs);
  }

  .section-subtitle {
    font-size: var(--font-size-lg);
    margin-bottom: var(--space-xl);
    max-width: 600px;
  }

  .section-alt {
    background: var(--color-bg-secondary);
  }

  /* Assess Grid */
  .assess-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-md);
  }

  .assess-card {
    padding: var(--space-lg);
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 8px;
  }

  .assess-card h3 {
    font-size: var(--font-size-lg);
    margin-bottom: var(--space-sm);
  }

  .assess-card p {
    font-size: var(--font-size-sm);
    margin: 0;
  }

  /* Context Grid */
  .context-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
    align-items: start;
  }

  .context-content h2 {
    margin-bottom: var(--space-md);
  }

  .context-content p {
    margin-bottom: var(--space-md);
  }

  .context-cards {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .context-card {
    padding: var(--space-md);
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 8px;
  }

  .context-card h4 {
    font-size: var(--font-size-base);
    margin-bottom: var(--space-xs);
  }

  .context-card p {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    margin: 0;
  }

  /* CTA */
  .section-cta {
    text-align: center;
    border-top: 1px solid var(--color-border);
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-content h2 {
    margin-bottom: var(--space-md);
  }

  .cta-content p {
    font-size: var(--font-size-lg);
    margin-bottom: var(--space-lg);
    margin-left: auto;
    margin-right: auto;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .assess-grid {
      gap: var(--space-sm);
    }
  }

  @media (max-width: 768px) {
    .product-grid {
      grid-template-columns: 1fr;
    }

    .assess-grid {
      grid-template-columns: 1fr;
    }

    .context-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
