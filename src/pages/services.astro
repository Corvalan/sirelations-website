---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Discernment Snapshot" description="A bounded assessment of human discernment in one decision context where superintelligent systems play a role. 72 hours. Memo + Checklist.">
  <section class="hero-small">
    <div class="container">
      <h1>The Discernment Snapshot</h1>
      <p>Measure where you stand. Know what to do about it.</p>
    </div>
  </section>

  <!-- Main Product -->
  <section class="section">
    <div class="container">
      <div class="product-intro">
        <div class="product-content">
          <h2>What it is</h2>
          <p class="mt-md">
            A bounded, decision-ready assessment of human discernment in one concrete
            decision context where superintelligent systems play a role — as visible expert,
            as agentic team, as embedded infrastructure, or as external influence.
          </p>
          <p class="mt-md">
            Not a generic AI audit. Not a compliance checklist. A focused measurement
            of whether the humans involved can actually steer.
          </p>
        </div>
        <div class="product-specs">
          <div class="spec-card">
            <div class="spec-label">Scope</div>
            <div class="spec-value">One decision context</div>
          </div>
          <div class="spec-card">
            <div class="spec-label">Turnaround</div>
            <div class="spec-value">72 hours</div>
          </div>
          <div class="spec-card">
            <div class="spec-label">Output</div>
            <div class="spec-value">Memo + Checklist</div>
          </div>
          <div class="spec-card">
            <div class="spec-label">Price</div>
            <div class="spec-value">€450</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Process -->
  <section class="section approach-section">
    <div class="container">
      <div class="text-center">
        <h2>How it works</h2>
      </div>

      <div class="approach-steps mt-xl">
        <div class="step">
          <div class="step-number">1</div>
          <h3>Intake</h3>
          <p>Async questionnaire about decision context, system influences, and friction points (~15 min)</p>
        </div>
        <div class="step-connector"></div>
        <div class="step">
          <div class="step-number">2</div>
          <h3>Session</h3>
          <p>Structured online analysis of how decision-making operates under superintelligent pressure, including mini-simulation (45-60 min)</p>
        </div>
        <div class="step-connector"></div>
        <div class="step">
          <div class="step-number">3</div>
          <h3>Demo</h3>
          <p>Live demonstration of the Discernment.ai measurement chain — proof that the method is concrete and reproducible (10-15 min)</p>
        </div>
        <div class="step-connector"></div>
        <div class="step">
          <div class="step-number">4</div>
          <h3>Output</h3>
          <p>Written deliverables within 72 hours: Memo (3-5 pages) + Checklist (1 page)</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Deliverables -->
  <section class="section">
    <div class="container">
      <div class="text-center">
        <h2>What you get</h2>
      </div>

      <div class="deliverables-grid mt-xl">
        <div class="deliverable-card">
          <div class="deliverable-icon">📄</div>
          <h3>Memo (3-5 pages)</h3>
          <ul class="deliverable-list">
            <li>Patterns identified in the decision context</li>
            <li>Vulnerabilities where AI influence is unnoticed</li>
            <li>Decision bottlenecks where responsibility diffuses</li>
            <li>Development opportunities for strengthening discernment</li>
            <li>Explicit assumptions and scope boundaries</li>
          </ul>
        </div>

        <div class="deliverable-card">
          <div class="deliverable-icon">✅</div>
          <h3>Checklist (1 page)</h3>
          <ul class="deliverable-list">
            <li>Concrete discernment checks for immediate use</li>
            <li>Decision agreements for the team</li>
            <li>When to pause and verify</li>
            <li>What to document and why</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Not Included -->
  <section class="section not-section">
    <div class="container">
      <div class="text-center">
        <h2>What this is not</h2>
        <p class="mt-md">Clarity about boundaries is part of the discipline.</p>
      </div>

      <div class="not-grid mt-xl">
        <div class="not-item">
          <span class="not-icon">✗</span>
          <div>
            <h3>Not an implementation project</h3>
            <p>We measure and advise. We don't redesign your processes.</p>
          </div>
        </div>
        <div class="not-item">
          <span class="not-icon">✗</span>
          <div>
            <h3>Not a legal audit</h3>
            <p>We assess human capacity, not legal compliance. The two are related but distinct.</p>
          </div>
        </div>
        <div class="not-item">
          <span class="not-icon">✗</span>
          <div>
            <h3>Not a certification</h3>
            <p>The Snapshot is a measurement artifact, not a stamp of approval.</p>
          </div>
        </div>
        <div class="not-item">
          <span class="not-icon">✗</span>
          <div>
            <h3>Not generic AI training</h3>
            <p>We don't teach "AI literacy" in general. We measure discernment in your specific context.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Regulatory Fit -->
  <section class="section">
    <div class="container">
      <div class="text-center">
        <h2>Regulatory alignment</h2>
        <p class="mt-md">The Snapshot supports compliance — but from human capacity, not checkbox completion.</p>
      </div>

      <div class="reg-fit-grid mt-xl">
        <div class="reg-fit-card">
          <h3>EU AI Act Article 14</h3>
          <p>Requires humans to "correctly interpret" AI output and decide when to override. The Snapshot measures whether they can.</p>
        </div>
        <div class="reg-fit-card">
          <h3>EU AI Act Article 4</h3>
          <p>Mandates AI literacy. The Snapshot goes further: not just knowledge, but demonstrated discernment under pressure.</p>
        </div>
        <div class="reg-fit-card">
          <h3>Dutch FRAIA</h3>
          <p>Impact assessments identify risks. The Snapshot tests whether the humans doing oversight have the capacity to see them in practice.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- On-ramp -->
  <section class="section onramp-section">
    <div class="container">
      <div class="onramp-grid">
        <div class="onramp-content">
          <h2>Not sure yet?</h2>
          <p class="mt-md">
            <strong>On-ramp</strong> — a lighter entry point. Short intake, focused conversation,
            mini-memo (1 page: 3 observations + 3 actions). 48 hours. €100.
          </p>
          <p class="mt-md">
            Use it to test whether the Snapshot is the right next step for your situation.
          </p>
        </div>
        <div class="onramp-cta">
          <a href="/contact" class="btn btn-secondary">Ask about On-ramp</a>
        </div>
      </div>
    </div>
  </section>

  <section class="section cta-section">
    <div class="container text-center">
      <h2>Ready to find out where you stand?</h2>
      <p class="mt-md">One decision context. One focused assessment. Actionable output.</p>
      <a href="/contact" class="btn btn-primary mt-lg">Request a Snapshot</a>
    </div>
  </section>
</BaseLayout>

<style>
  .hero-small {
    padding: var(--space-xl) 0;
    border-bottom: 1px solid var(--color-border);
  }

  .hero-small h1 {
    font-size: 2.5rem;
    margin-bottom: var(--space-sm);
  }

  .hero-small p {
    font-size: 1.125rem;
  }

  .product-intro {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: var(--space-xl);
    align-items: start;
  }

  .product-content h2 {
    color: var(--color-text);
  }

  .product-content p {
    font-size: 1.0625rem;
    line-height: 1.7;
  }

  .product-specs {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-md);
  }

  .spec-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: var(--space-lg);
    text-align: center;
  }

  .spec-label {
    font-size: 0.75rem;
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--space-xs);
  }

  .spec-value {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text);
  }

  .approach-section {
    background: var(--color-bg-secondary);
  }

  .approach-steps {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .step {
    flex: 1;
    text-align: center;
    max-width: 220px;
  }

  .step-number {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-accent);
    color: white;
    font-weight: 600;
    border-radius: 50%;
    margin: 0 auto var(--space-md);
  }

  .step h3 {
    color: var(--color-text);
    margin-bottom: var(--space-sm);
  }

  .step p {
    font-size: 0.875rem;
    line-height: 1.5;
  }

  .step-connector {
    flex: 0.5;
    height: 2px;
    background: var(--color-border);
    margin-top: 24px;
    max-width: 60px;
  }

  .deliverables-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
  }

  .deliverable-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: var(--space-xl);
  }

  .deliverable-icon {
    font-size: 2.5rem;
    margin-bottom: var(--space-md);
  }

  .deliverable-card h3 {
    color: var(--color-text);
    margin-bottom: var(--space-md);
  }

  .deliverable-list {
    list-style: none;
  }

  .deliverable-list li {
    position: relative;
    padding-left: 1.5rem;
    margin-bottom: var(--space-sm);
    font-size: 0.875rem;
  }

  .deliverable-list li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--color-accent);
  }

  .not-section {
    background: var(--color-bg-secondary);
  }

  .not-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
  }

  .not-item {
    display: flex;
    gap: var(--space-md);
    align-items: flex-start;
  }

  .not-icon {
    color: var(--color-text-muted);
    font-size: 1.25rem;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .not-item h3 {
    color: var(--color-text);
    font-size: 1rem;
    margin-bottom: var(--space-xs);
  }

  .not-item p {
    font-size: 0.875rem;
  }

  .reg-fit-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .reg-fit-card {
    padding: var(--space-lg);
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
  }

  .reg-fit-card h3 {
    color: var(--color-text);
    font-size: 1rem;
    margin-bottom: var(--space-sm);
  }

  .reg-fit-card p {
    font-size: 0.875rem;
  }

  .onramp-section {
    background: var(--color-bg-secondary);
  }

  .onramp-grid {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-xl);
  }

  .onramp-content {
    max-width: 600px;
  }

  .onramp-content h2 {
    color: var(--color-text);
  }

  .onramp-content p {
    font-size: 1rem;
    line-height: 1.6;
  }

  .cta-section {
    border-top: 1px solid var(--color-border);
  }

  @media (max-width: 1024px) {
    .approach-steps {
      flex-wrap: wrap;
      justify-content: center;
      gap: var(--space-lg);
    }

    .step-connector {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .product-intro {
      grid-template-columns: 1fr;
    }

    .deliverables-grid {
      grid-template-columns: 1fr;
    }

    .not-grid {
      grid-template-columns: 1fr;
    }

    .reg-fit-grid {
      grid-template-columns: 1fr;
    }

    .onramp-grid {
      flex-direction: column;
      text-align: center;
    }

    .approach-steps {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
